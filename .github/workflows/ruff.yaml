on: [push]

jobs:
  ruff:
    runs-on: ubuntu-latest
    name: "ruff"
    steps:
      - name: actions_python_ruff
        uses: davidslusser/actions_python_ruff@2-bug-use-ruff-check-now
        with:
            src: "src/django_project"
            options: "-v"
            python_version: "3.11"