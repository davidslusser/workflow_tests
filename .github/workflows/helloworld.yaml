on: [push]

jobs:
  coveralls:
    runs-on: ubuntu-latest
    name: "coveralls"
    steps:
      - uses: davidslusser/hello-world-composite-action@main
        with:
          src: "src"
          coveralls_repo_token: ${{ secrets.COVERALLS_REPO_TOKEN }}
